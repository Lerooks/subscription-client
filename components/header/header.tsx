import React, { ReactElement } from 'react'
import * as S from './header-styles'

export default function Header(): ReactElement {
  return (
    <S.Header>
      <S.Title> Tournament </S.Title>
    </S.Header>
  )
}
